# SSDLC RSA Encryption Service — Environment Configuration
# NEVER commit actual secrets. This file is a TEMPLATE only.

# Application
APP_ENV=production
APP_HOST=0.0.0.0
APP_PORT=8443
DEBUG=false

# Database
DATABASE_URL=sqlite+aiosqlite:///./data/encryption_service.db

# JWT Authentication
JWT_SECRET_KEY=CHANGE_ME_TO_A_RANDOM_64_CHAR_HEX_STRING
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_MINUTES=10080

# RSA Key Configuration
RSA_KEY_SIZE=4096
RSA_PUBLIC_EXPONENT=65537

# File Storage
UPLOAD_DIR=./data/uploads
ENCRYPTED_DIR=./data/encrypted
MAX_FILE_SIZE_MB=50

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log

# TLS (for production)
TLS_CERT_PATH=./certs/server.crt
TLS_KEY_PATH=./certs/server.key
